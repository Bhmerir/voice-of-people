<section class="container ">
    {{!-- <section class="flex-row justify-center"> --}}
    <section class="post">
        {{#each posts as |post|}}
        {{> post-detail}}
        {{/each}}
    </section>
</section>